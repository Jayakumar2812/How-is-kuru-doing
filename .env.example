MONAD_RPC_URL=
MONAD_TRACE_RPC_URL=
# Optional for local dev after `vercel env pull` (auto on Vercel deploy)
BLOB_READ_WRITE_TOKEN=
# Required for daily margin-flows cron (Vercel sends Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=
# Optional RPC tuning (defaults shown)
# RPC_MAX_CONCURRENT=4
# RPC_MIN_GAP_MS=250
# RPC_TRACE_MAX_CONCURRENT=2
# RPC_TRACE_MIN_GAP_MS=500
# RPC_BATCH_SIZE=20
# RPC_TRACE_BATCH_SIZE=4
# RPC_LOGS_CONCURRENCY=4
